
## video_monitor.html ##

Adapt videos size to the window in height and width to fit in the browser wepage.
A standalone video cannot be larger to taller than the browser windows (-top bar)



## APPLICATION MANAGEMENT ##

Create functions to manage known applications
- Known applications are listed in data/app_names.csv (information filled in config.json)
Cached icons will be stored in website\assets\app_icons

Get-HeadsetInstalledApps should connect to the headset and get an array of all apps installed (PackageName)

<#
Function Search-AppInfoOnTheWeb
	input parameter is the PackageName and known application app_names file
	It should search on the github repository "https://raw.githubusercontent.com/threethan/MetaMetadata/main/data" the $PackageName.json
	If found, return the array with PackageName,DisplayName,IconUrl
	The iconUrl should be square if exists. Otherwise let's find another icon.
#>
	
Get-AppInfo will replace Get-AppDisplayName.
	input parameter is the PackageName
	Cache file will be stored in app_infos.csv by default, but this file name should be defined in config.json
		it contains PackageName,DisplayName,IconUrl,LocalIconPath
	Cached icons will be stored in website\assets\app_icons (static assignment)
		
	The function reads from cache files first. 		
		While not found, or displayname = packageName or if missing raw value : search information using Search-AppInfoOnTheWeb
			if result is different than cached values, update it
			if icon is not already in app_icons cached folder, let's download it, and update LocalIconPath
	
	if found and raws have a value let's return the values array.
	If not found, return array with DisplayName = (PackageName - "com." at the begining)
	update the app_infos file


Use Get-AppInfo while listing installed applications, while displaying the running application, or while listing apps to start on a headset.



	





Add icons of the running application before the name

##########


If the headset is connected using USB cable, let's get stream and any other information from USB instead of Wifi connection.
Maybe that needs to check first of the loop for connection to the headset object of it's connected in USB first, and the, 


Setup few parametes to manage on the headset :
- Time before shutting down the screen
- proximity sensor activation/desactivation
- Guardian : Change detection of the limit - stationary, Guardian, enable/disable travel mode
- Switch to Passthrough / VR mode
- Switch hand tracking/controllers


While leaving, the app restarts by itself sometimes

When scrcpy is already running, if I switch to anoter desktop on Windows 11, it automatically start new scrcpy windows. It shouldn't.
>> Tried but it failed

Let's see if it's possible to know if the headset is charging or discharging, even if the headset is connected to power.
Then try to identify how long the headset can remain up an running based on the battery level, and the discharging speed.

Implement in main video and on the focused video screen the timer based on the file placed in /website/timer
Let's check with stream deck activity

# video_monitor.html
Drag and drop works to re-order when getting from right to left, but not from left to right.

In the app_config.html implement a button to edit config.json in the web page.
It should display each functions and values.

While delete a headset, delete [headset]_installed_apps.csv and [headset]favorite_apps.csv if not already done


DEBUG
under headset_settings.htlm, you were implementing the drag and drop functionality.
The implementation partially works, but you had a "fix drag-and-drop JS (mousedown toggle pattern)" step to manage, and you didn't finished this step. Can you check related code ?
While dra drop to the left, it's working. While drag drop to the right, it's not reordering headsets