Installed applications name missing : should be searched probably also in data/common : data/common/com.kluge.SynthRiders.json 

Add icons of the running application before the name


If the headset is connected using USB cable, let's get stream and any other information from USB instead of Wifi connection.
Maybe that needs to check first of the loop for connection to the headset object of it's connected in USB first, and the, 


Setup few parametes to manage on the headset :
- Time before shutting down the screen
- proximity sensor activation/desactivation
- Guardian : Change detection of the limit - stationary, Guardian, enable/disable travel mode
- Switch to Passthrough / VR mode
- Switch hand tracking/controllers

    In config button in the top bar :
    Replace Headset Actions section by Help button.
    - In this new page, add a button to restart Web server, MediaMTX server, and list all links that can be usefull for troubleshooting web server or mediamtx app.
    - Add a link to the github web page : https://github.com/lyon-esport/VR_HEADSET_MANAGER
    Add anoter buttin in help button for app Configuration
    Here, let's add a buttons to :
    - Open and edit the config.json file
    - Configure path to records (and a way to open it)
    - Open logs folder and another to display last logs dynamically/Paused


While leaving, the app restarts by itself sometimes

When scrcpy is already running, if I switch to anoter desktop on Windows 11, it automatically start new scrcpy windows. It shouldn't.
>> Tried but it failed

Let's see if it's possible to know if the headset is charging or discharging, even if the headset is connected to power.
Then try to identify how long the headset can remain up an running based on the battery level, and the discharging speed.

Implement in main video and on the focused video screen the timer based on the file placed in /website/timer
Let's check with stream deck activity


In headset_settings.html implement a way to reorder headsets (drag and drop of the headset parameters window ?)


