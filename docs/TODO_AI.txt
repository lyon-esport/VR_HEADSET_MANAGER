ALL CHANGES AND UPDATES TO MANAGE WITH AI TOOLS




- On startup, it requests to install NuGet. See if we can drop this by getting all imported modules in local without NuGet usage
Après téléchargement de ffmpeg.exe, : Prompt pour fournisseur NuGet
>> Voir si utilisation de NuGet obligatoire ou si on peut s'en passer.

- Get-NetFirewall Rules sort une erreur au démarrage, mais démarre quand même la page en admin pour ouvrir le trafic sur le pare-feu

- Ajouter un auto refrersh dans la fenêtre d'ajout d'un casque (pour afficher le casque s'il a été connecté entre temps)

- Generate unit test scripts to test
	- Syntax errors by passing the full file path as argument
	- restart webserver while it's necessary
	- Web server working
	- New web page
	- Test erros 


PB 
Switcher sur le mode portrait bloque le restream sur mediaMTX




## video_monitor.html ##


## APPLICATION MANAGEMENT ##



##########


When scrcpy is already running, if I switch to anoter desktop on Windows 11, it automatically start new scrcpy windows. It shouldn't.
>> Tried but it failed
>> Maybe by ensuring the page is always in all desktops?
>> Also try to get these pages in background while starting, 

Let's see if it's possible to know if the headset is charging or discharging, even if the headset is connected to power.
Then try to identify how long the headset can remain up an running based on the battery level, and the discharging speed in Watt.
Also, if the headset is charging, let's return the remaining time until the headset will be recharged at 100%.


Let's check with stream deck activity




# headset_settings.html

	If there is no headset in the list, let's redirect the main webpage to the headset_settings web page
	In the headset_settings web page, if there is no headset, let's add a button "add new headset"
	

	Please generate a function like computer monitoring, in utils.ps1 that will call all other functions for collecting computer workload.
	This will place the results in a computer_monitoring.json file that will be stored in data.
	It will keep only the last check results in the file. This file will be overwritten every time.
	It should also contains count of currently running scrcpy jobs (this should be easy to get, if there isn't already a function for that in scrcpy_launcher.ps1, let's create it)

	This funciton will be triggeered by the headsets monitoring loop (that runs every 5s) to check 1 time per min the computer workload.
	This function will start if the json file has been generated for more than 1 min.
	A flag must exist to force re-check of the computer workload before the 1min "waiting time" before refresh. This will be used later, for example from the web interface or from the console.
	The timing of 1min of waiting time before doing a computer monitoring refresh, and the file name of the json, should be customizable and will be configured in the config file and loaded as a global variable, and will be updateble from the web interface in Edit config File menu.

In headsets_monitoring.html add a drop-down section below headsets list.
This drop-down list will display all computer statistics collected in the $global:computerMonitoringFilePath 



#### RELEASE ####

	Clear all local files (see scripts/Prepare-Release.ps1)
	Clear useless Sources files
	Check a way to dl ffmpeg directly while lauching the app on the first time, and install it on computer ?
	Find a way to install the software, or have a better fw checks while launching and starting as admin automatically in another page.
	If ports changes, need to remove previous fw entry, and recreate entries with the new rules
	
	On first launch (=no config file found) :
		- Welcome menu/web page ?
		- Choose recording folder
		- Choose Wifi SSID and password
		- Manage ffmpeg download manual/automatic https://gist.github.com/AnjanaMadu/5f9689e9572492a50089f4a74b9b8de5



DEBUG

	
	Meta quest New UI :
		Check apps names and update names in known apps.
	
	size of generated [monitor].html headset monitor data to fix (currently splitting to another line if remaining time is not correct...)
	Maybe base the remaining time to the last 2 measures instead of 3 ?


	
	
	To burn tokens...
		Find a way to start adbwifi on headset startup... Lets dev an app ?
	
	