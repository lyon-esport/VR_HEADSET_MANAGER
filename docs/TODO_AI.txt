
## video_monitor.html ##

Create a new functionaility (and a new ps1 module for all future related functions) named timer.
For each headset, let's generate a timer based on a text file. The timer file name is based on the headset id, and is stored in /website/timer
in headset_settings.html let's add a new section in each headset, between recording and power sections
This timer section should :
- Define the timer value in min:sec
- Have a switch button if it's a incremental of decremental timer (pressing displays inc (0 to end of timer) or dec (timer value to 0)), and a button to press for starting the update.
- Have a button to start the timer

Everytime the button is pressed, the text in the file should update with the remaining time like 06:30, 06:29 etc, and must be updated every seconds until 0. At 0 (or timer value reached if inc mode) it should display "Time's up !" 

Add the visibility of the timer in [video].html genreated captures, and in the main video_monitor.html generated file.
The visibility should read the text file and update dynamically the value on screen while the timer file content changes.
The timer will be only white text, centered, on the botom of the video capture.
Timer visibility should be enabled or disabled with a new button (with a clock icon) like monitoring information.


## APPLICATION MANAGEMENT ##

Add icons of the running application before the name

##########


Setup few parametes to manage on the headset :
- Time before shutting down the screen
- proximity sensor activation/desactivation
- Guardian : Change detection of the limit - stationary, Guardian, enable/disable travel mode
- Switch to Passthrough / VR mode
- Switch hand tracking/controllers


When scrcpy is already running, if I switch to anoter desktop on Windows 11, it automatically start new scrcpy windows. It shouldn't.
>> Tried but it failed
>> Maybe by ensuring the page is always in all desktops?
>> Also try to get these pages in background while starting, 

Let's see if it's possible to know if the headset is charging or discharging, even if the headset is connected to power.
Then try to identify how long the headset can remain up an running based on the battery level, and the discharging speed in Watt.
Also, if the headset is charging, let's return the remaining time until the headset will be recharged at 100%.



Let's check with stream deck activity




# headset_settings.html




#### RELEASE ####
	Clear all local files (see scripts/Prepare-Release.ps1)
	Clear useless Sources files
	Check a way to dl ffmpeg directly while lauching the app on the first time, and install it on computer ?
	Find a way to install the software, or have a better fw checks while launching and starting as admin automatically in another page.
	If ports changes, need to remove previous fw entry, and recreate entries with the new rules
	


DEBUG

	Console : "Web Monitor available at :"
		list @IP + interface : LAN/WAN (+ speed)
	
	Meta quest New UI :
		Check apps names and update names in known apps.
	
	size of generated [monitor].html headset monitor data to fix (currently splitting to another line if remaining time is not correct...)
	Maybe base the remaining time to the last 2 measures instead of 3 ?


	
	
	To burn tokens...
		Find a way to start adbwifi on headset startup...
	
	